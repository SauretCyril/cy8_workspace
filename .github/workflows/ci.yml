name: CI - Tests et Validation

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # Tests Ubuntu temporairement désactivés - Focus sur Windows uniquement
  # test:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       python-version: [3.9, 3.10, 3.11, 3.12]
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Set up Python ${{ matrix.python-version }}
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: ${{ matrix.python-version }}
  #
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt
  #         pip install pytest pytest-cov flake8 black
  #
  #     - name: Lint with flake8
  #       run: |
  #         # Stop the build if there are Python syntax errors or undefined names
  #         flake8 src/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics
  #         # Exit-zero treats all errors as warnings. GitHub editor is 127 chars wide
  #         flake8 src/ tests/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  #
  #     - name: Check code formatting with black
  #       run: |
  #         black --check src/ tests/
  #
  #     - name: Run cy8 test suite
  #       run: |
  #         cd ${{ github.workspace }}
  #         python src/cy8_test_suite.py
  #
  #     - name: Run pytest tests
  #       run: |
  #         pytest tests/ -v --cov=src --cov-report=xml --cov-report=term-missing
  #
  #     - name: Upload coverage to Codecov
  #       uses: codecov/codecov-action@v3
  #       with:
  #         file: ./coverage.xml
  #         flags: unittests
  #         name: codecov-umbrella
  #         fail_ci_if_error: false

  windows-test:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: [3.10, 3.11]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov flake8 black

      - name: Lint with flake8
        run: |
          flake8 src/ tests/ --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 src/ tests/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Check code formatting with black
        run: |
          black --check src/ tests/

      - name: Run cy8 test suite on Windows
        run: |
          python src/cy8_test_suite.py

      - name: Run pytest tests on Windows
        run: |
          pytest tests/ -v --cov=src --cov-report=xml --cov-report=term-missing

      - name: Upload coverage to Codecov (Windows)
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          flags: windows-tests
          name: codecov-windows
          fail_ci_if_error: false
